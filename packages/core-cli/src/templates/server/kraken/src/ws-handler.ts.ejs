import { wsHandler } from '@kraken.js/aws-graphql';
import { schema: awsDirectives } from "@kraken.js/aws-directives";

export const handler = wsHandler({
    imports: [
        awsDirectives
    ]
});
