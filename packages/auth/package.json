{
  "name": "@kraken.js/auth",
  "version": "0.0.5",
  "license": "MIT",
  "author": "@kraken.js",
  "repository": "github:kraken-js/kraken.js",
  "main": "dist/main.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "pretest": "npx rimraf dist",
    "test": "jest --passWithNoTests --runInBand",
    "prepackage": "npx rimraf dist",
    "package": "webpack-cli",
    "emit-declarations-only": "tsc --emitDeclarationOnly",
    "prepublishOnly": "yarn package && yarn emit-declarations-only"
  },
  "files": [
    "dist",
    "package.json",
    "tsconfig.json",
    "README.md",
    "LICENSE",
    "src/schema.graphql"
  ],
  "devDependencies": {
    "@kraken.js/jest": "0.0.5",
    "@kraken.js/webpack": "0.0.5",
    "jest": "^26.4.2"
  },
  "peerDependencies": {
    "graphql": "^15.0.0"
  },
  "size-limit": [
    {
      "gzip": false,
      "limit": "350 KB",
      "ignore": [
        "@kraken.js/core",
        "aws-sdk"
      ]
    }
  ]
}
