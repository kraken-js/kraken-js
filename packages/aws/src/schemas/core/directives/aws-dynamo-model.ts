import { SchemaDirectiveVisitor } from '@graphql-tools/utils';

export class AwsDynamoModelDirective extends SchemaDirectiveVisitor {
  visitObject() {
    // ¯\_(ツ)_/¯ nothing to do here but still needs to implement it :)
  }
}
